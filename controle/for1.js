let contador = 1 //Declaração de uma variável//
while (contador <= 10) { // expressão de uma variável// controla o laço
    console.log(`contador = ${contador}`)
    contador++ // incremento de uma variável// operador unário
}

// mesma função de sima. 

for (let i = 1; i <= 10; i++) {
    console.log(`i = ${i}`)
}
const notas = [6.7, 7.4, 9.8, 7.7] // expressão em array
for (let i = 0; i < notas.length; i++) { //começe com o "0", vá até o "i" menor do que o arrei e acrecente 1//
    console.log(`nota = ${notas[i]}`)
}