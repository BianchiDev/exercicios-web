// calcule a área de uma circunferência.
const raio = 5.6
const area = Math.PI * Math.pow(raio, 2)
const comprimento = 2 * Math.PI * raio

console.log(comprimento)
console.log(area)
console.log(typeof Math) // Math é um objeto
    // Math "tradução" Matemática.