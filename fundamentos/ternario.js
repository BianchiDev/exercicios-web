// operadores ternários.
const resutado = nota => nota >= 7 ? 'Aprovado' : 'reprovado' // the function arey the return is implicit
console.log(resutado(7.1))
console.log(resutado(6.7))
    // operador ternário "?" nota >= 7 primeira parte. Se de false "reprovado" se trus " aprovado"