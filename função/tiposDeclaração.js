//function declaration " forma tradicional"
function soma(x, y) {
    return x + y
}

//function expression " declara e atribui a uma constante ou variável "
const sub = function(x, y) {
    return x - y
}

// named function expression " para debugar o código "
const mult = function mult(x, y) {
    return x * y
}